NEW ERIDIAN — STREAMELEMENTS DASHBOARD SETUP
Replace YOUR-API-DOMAIN and YOUR_ADMIN_KEY first.
Access 100 = Everyone, 500 = Moderator.

!seed
Response: 🌱 NEW ERIDIAN // SEED CHAT GAME | !start !me !job !skills !inventory !farm !harvest !mine !rare !research !craft !build !work !cargo !delivery !explore !market !status !progress !event !siro !rocky
Access: 100 | Global CD: 15s | User CD: 30s

!start
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/start?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 10s | User CD: 86400s

!me
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/profile?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender)))
Access: 100 | Global CD: 5s | User CD: 15s

!skills
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/skills?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender)))
Access: 100 | Global CD: 5s | User CD: 15s

!inventory
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/inventory?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender)))
Access: 100 | Global CD: 5s | User CD: 15s

!wallet
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/wallet?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender)))
Access: 100 | Global CD: 5s | User CD: 15s

!job
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/job?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&job=$(queryescape $(1)))
Access: 100 | Global CD: 5s | User CD: 30s

!farm
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/farm?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 30s

!harvest
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/harvest?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 35s

!mine
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/mine?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 35s

!rare
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/rare?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 5s | User CD: 90s

!research
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/research?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 35s

!craft
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/craft?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 45s

!build
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/build?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 45s

!work
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/work?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 35s

!cargo
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/cargo?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 45s

!delivery
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/delivery?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 45s

!explore
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/explore?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 50s

!market
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/market?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 45s

!eat
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/eat?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 15s

!sleep
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/action/sleep?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender))&msg=$(msgid))
Access: 100 | Global CD: 3s | User CD: 60s

!status
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/society?channel=$(channel.provider_id))
Access: 100 | Global CD: 8s | User CD: 20s

!progress
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/progress?channel=$(channel.provider_id))
Access: 100 | Global CD: 8s | User CD: 20s

!event
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/event?channel=$(channel.provider_id))
Access: 100 | Global CD: 8s | User CD: 20s

!rocky
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/rocky?channel=$(channel.provider_id)&uid=$(sender.twitchid)&name=$(queryescape $(sender)))
Access: 100 | Global CD: 8s | User CD: 60s

!siro
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/siro?channel=$(channel.provider_id))
Access: 100 | Global CD: 8s | User CD: 60s

!sirostart
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/siro/on?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!siroend
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/siro/off?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!foodcrisis
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/food/on?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!foodclear
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/food/off?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!miningboom
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/mining/on?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!miningclear
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/mining/off?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!deliverysurge
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/delivery/on?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!deliveryclear
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/delivery/off?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!marketboom
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/market/on?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!marketclear
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/event/market/off?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

!nextday
Response: $(customapi https://YOUR-API-DOMAIN/api/v1/admin/day/next?channel=$(channel.provider_id)&level=$(sender.level)&key=YOUR_ADMIN_KEY)
Access: 500 | Global CD: 5s | User CD: 5s

